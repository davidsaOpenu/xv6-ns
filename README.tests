The following command:

   ./run_all_regressions.expect log.txt

will spawn 'make qemu-nox', execute run_all_regressions.sh from the xv6 command line,
log the output to the log.txt and, finally, kill the qemu process.
